# Todo

Add to-do here if you want or github issue

- Support for different canvas types/behavior for example pixelated canvas or line canvas
- [x] Refractor core engine into modular structure — see `source/` module layout in README.md
- Add canvas receiver example
- Add local canvas support in the engine (no pubnub)
